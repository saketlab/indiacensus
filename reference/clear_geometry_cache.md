# Clear geometry cache

Clear geometry cache

## Usage

``` r
clear_geometry_cache()
```

## Value

Invisible NULL

## Examples

``` r
if (FALSE) { # \dontrun{
# Clear cached geometry to free memory
clear_geometry_cache()
} # }
```
